## Fixes
- Log viewer: the end of the log no longer changes depending on the selected line count (200 / 500 / 1000 / 5000). The container backlog is now shown in true chronological order — a container's stderr lines (such as a Java app's startup warnings) are no longer shoved to the bottom of the log regardless of when they were written.
