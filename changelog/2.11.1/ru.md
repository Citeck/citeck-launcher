## Исправления
- **Windows: рядом с лаунчером больше не открывается консольное окно.** Приложение при каждом запуске открывало второе, чёрное окно; теперь его нет, а собственный лог лаунчера пишется в `launcher.log` в папке логов.
- **При создании мастер-пароля теперь видно, что пароли не совпадают.** Проверка и раньше не давала продолжить, но ничего не сообщала — из-за этого кнопка «Подтвердить» выглядела нерабочей.

## Улучшения
- Библиотека Git обновлена до 5.19.2: закрыты две уязвимости в работе с репозиторием — выход за пределы каталога через специально составленные имена ссылок и переход по символическим ссылкам при операциях с рабочим деревом.
