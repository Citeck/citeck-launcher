## 修复
- **从 1.x 升级不会再让已删除的命名空间重新出现。** 它们在列表中显示为没有名称、没有 bundle 的裸标识符，打开时会报 `namespace "..." not found`。在 1.x 中删除命名空间会残留一部分状态，升级时每份残留都变成了一条独立记录。如果列表中已经出现这些记录，用垃圾桶图标删除即可，其他内容不受影响。
- 旧的默认工作区中的命名空间在升级后会保留已停止应用的状态和记住的 bundle。
