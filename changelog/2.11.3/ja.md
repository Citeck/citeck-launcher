## 修正
- **1.x からの移行で、削除済みのネームスペースが復活しなくなりました。** 名前も bundle もない ID だけの項目として一覧に現れ、開くと `namespace "..." not found` になっていました。1.x でネームスペースを削除しても状態の一部が残り、移行時にその残骸がそれぞれ 1 件の項目になっていたためです。すでに一覧に出ている場合はゴミ箱アイコンで削除してください。ほかへの影響はありません。
- 従来の既定ワークスペースのネームスペースが、移行後も停止中アプリの状態と記憶された bundle を保持するようになりました。
