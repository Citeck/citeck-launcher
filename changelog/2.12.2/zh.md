## 修复
- **修复从 2.12.0 更新到 2.12.1 后启动器损坏的问题。** 更新只替换守护进程，桌面应用本身仍是您安装的版本；而 2.12.1 的守护进程在启动过程中会用一段 JSON 拒绝回应对窗口的请求，旧版应用就把这段 JSON 显示出来并一直停在那里。现在正在启动的守护进程会返回加载指示，窗口稍后会自行恢复。如果您已经遇到显示 JSON 的白屏，请重新安装启动器（或删除启动器数据目录下的 `updates` 文件夹）——本次发布可防止问题再次发生。
