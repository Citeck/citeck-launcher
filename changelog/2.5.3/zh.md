## 修复
- 修复了创建/编辑命名空间对话框中的下拉菜单（捆绑包仓库、版本、认证类型）无法打开的问题。
- 新发布的平台版本重新出现在版本选择器中：过时的本地工作区副本不再将其隐藏，快速启动现在显示解析后的版本而非“LATEST”。
- 在 Linux 上安装或更新后，应用程序菜单图标不再消失。
- 桌面更新后，主题和界面语言不再被重置。
- 桌面应用重新写入其守护进程日志文件（此前在桌面模式下为空），这是诊断问题所必需的。
