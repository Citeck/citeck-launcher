## Fixes
- Fixed dialogs closing unexpectedly when adding an item from a stacked dialog — for example, creating a new access token while setting up a workspace now completes reliably instead of closing every open dialog with nothing saved.
