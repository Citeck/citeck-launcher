## 修复
- 数据卷已被删除（例如在"卷"页面中删除）的依赖项，不再显示为可升级、而其迁移随后失败：已无数据需要保护，下次启动时将应用 bundle 中的版本。
- 当迁移需要更新的启动器时，`citeck deps` 现在提示重新运行 `install.sh`；`citeck update` 只会刷新 workspace 和 bundle 定义。
- 桌面版删除 namespace 前的警告现在会说明，其数据卷和目录（包括快照）也会被一并删除。
