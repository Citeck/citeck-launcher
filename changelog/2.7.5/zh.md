## 修复
- 停止命名空间现在会干净地移除其所有容器——停止较慢的应用（例如 eapps）在关闭后不再停留在 “Exited” 状态，命名空间网络也会被可靠地移除。
- 删除工作区现在会回收其中的所有内容——每个命名空间的 Docker 数据卷（PostgreSQL、MongoDB……）和数据库记录，而不仅仅是磁盘上的文件。
- 内置配置服务器（桌面）现在仅在命名空间处于活动状态时运行，并在停止时释放端口，因此在命名空间已停止的情况下重启应用不再因 “address already in use” 而失败。
