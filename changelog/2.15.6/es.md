## Correcciones
- Una imagen de una ruta del registro que el workspace declara sin autenticación (por ejemplo, un proyecto público de Harbor) se descarga de forma anónima y ya no pide credenciales, aunque el mismo registro también aloje imágenes privadas.
- Las credenciales del registro ya no se pierden cuando el workspace lista una ruta pública del mismo registro después de una privada.
