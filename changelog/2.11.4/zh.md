## 修复
- **通过齿轮图标修改应用版本后立即生效。** 编辑器显示的是新版本，但容器仍以旧版本运行，必须手动停止再启动才行。只有在切换到尚未拉取过的版本时才会出现，也就是每一次真正的版本升级。
- **切换 bundle 后顶部标题会同步更新。** 命名空间对话框显示新的 bundle，而顶部仍是旧的；命名空间处于停止状态时，直到重新打开该命名空间才会更正。
- **从 1.x 升级后，启动器会先询问主密码。** 此前命名空间会在迁移的密钥可读之前启动，于是镜像仓库登录窗口叠加在主密码输入框之上——既没有可选的密钥，也无法保存任何内容。
