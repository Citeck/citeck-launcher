## Fixes
- macOS: the Dock again shows the proper macOS-style app icon (a rounded square) instead of the bare round logo — the bundle icon was being overridden at startup.
- macOS: the menu bar item is now icon-only. The "Citeck Launcher" text no longer takes up a strip of the menu bar, and the icon is a monochrome glyph sized to match the system ones.
