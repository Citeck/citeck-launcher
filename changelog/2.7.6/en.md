## Fixes
- Creating an item from inside a stacked dialog no longer collapses every open dialog — for example, adding a new access token while creating a workspace now saves it correctly instead of closing all the dialogs with nothing created.
- Creating a workspace now automatically switches to it.
