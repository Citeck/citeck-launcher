## 修复
- **“更新并启动”在命名空间运行时恢复正常。** 此前点击没有任何效果：命名空间只是被重新加载，不会刷新 `:snapshot` 镜像，因此没有任何服务被重建。现在会拉取工作区和 bundle 仓库（遵循各自的更新周期）、刷新 snapshot 镜像，并且只重建配置或镜像确实发生变化的服务。
- **启动后服务不再长时间停留在“已停止”。** 此前在检查镜像期间，所有服务会持续显示“已停止”长达数十秒，之后状态才开始变化。现在启动一开始状态就会更新。
- **按钮不再像是毫无反应。** 在准备更新期间（拉取仓库、生成配置），按钮会显示正在工作；更新失败时也会说明原因。
