## 新功能
- 可复用的镜像仓库凭据。保存的私有仓库登录信息现在可以在多个命名空间和工作区之间复用，无需为每一个重新输入。此外，在命名空间启动前，启动器会检查所需的每个私有仓库都已配置凭据，从而提前发现缺失的登录信息，而不是在稍后拉取时卡住。

## 变更
- Bundle 选择器不再有单独的“LATEST”项：最新版本会标记为“(LATEST)”，命名空间始终固定到具体版本，因此不会自行切换到更新的版本。
