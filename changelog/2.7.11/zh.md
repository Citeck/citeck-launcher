## 修复
- 将 RabbitMQ 容器的内存上限从 512 MB 提高到 1 GB。在 enterprise 套件（24 个 Web 应用）中，消息代理稳定运行时的工作集超过了 `0.6` 高水位线（约 307 MB），触发内存资源告警并阻塞了所有消息发布者。提高上限后，水位线升至约 614 MB——明显高于 enterprise 的稳定用量——同时为容器的内存不足终止（OOM killer）保留了余量。该上限只是封顶，并非预留，因此较小的（community）部署不受影响。
