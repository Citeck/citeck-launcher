### Go ###
internal/daemon/webdist/

### Web ###
web/dist/
node_modules

### IDE ###
.idea
.vscode/
*.iws
*.iml
*.ipr

### OS ###
.DS_Store
*.log

### Legacy (Kotlin era) ###
.gradle
.kotlin

### Build artifacts ###
build/bin/
tools/h2-export/build/
# Kotlin/Gradle outputs from 1.x checkout (build/classes, build/libs, etc.) —
# 2.x is Go, so anything under top-level build/ that isn't bin/ is legacy noise.
build/classes/
build/generated/
build/intermediates/
build/kotlin/
build/libs/
build/reports/
build/resources/
build/tmp/

### H2 export tool (Gradle project — wrapper must be tracked) ###
!tools/h2-export/gradle/
!tools/h2-export/gradlew
!tools/h2-export/gradlew.bat

### Test ###
test-results/
coverage.out
coverage.html

### Playwright MCP ###
.playwright-mcp/

### Claude Code ###
.claude/

### Binaries (prevent accidental root-dir builds) ###
/citeck
/citeck-desktop

### Prompt files ###
PROMPT-*.md

### Audit artifacts (audit-project skill) ###
.audit-backlog.md

### Porting scratch (1.x → 2.x migration; not tracked) ###
docs/porting/

### Custom ###
*.png
!icons/**/*.png
!cmd/citeck-desktop/logo.png
